import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vinicanalles.finance',
  appName: 'FinTrack - Finanças Pessoais',
  webDir: 'dist'
};

export default config;
